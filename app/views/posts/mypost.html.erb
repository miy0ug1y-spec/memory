<h1>あなたの投稿一覧</h1>
<% @posts.each do |post| %>
 
  <%= link_to post.title, post_path(post.id) %>
   
  <%= post.body %>
  
  <% if post.image.attached? %>
    <%= image_tag post.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'letter.png', size: "200x200" %>
  <% end %>

<% end %>